# Build output of the plugin lives in the harness checkout, never here — but
# ignore it anyway in case a stray build runs inside plugin/.
plugin/lib/
plugin/node_modules/

# Generated by the probes; every one of them is rebuilt by a command in
# docs/probe-harness.md, so they are not worth tracking (they are also 25 MB).
_probe/frames.html
_probe/ring-preview.html
_probe/skin-lab.html
_probe/ring-placements.json
_probe/sprite-sheets.json
_probe/shots/frames-all.png
_probe/shots/skin-lab*.png
art/out/scan/

# OS / editor noise
.DS_Store
Thumbs.db
*.log
